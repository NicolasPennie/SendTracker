type Query {
    listSends: [Send]
}

enum Style {
    BOULDER,
    SPORT,
    TRAD
}

enum TickType {
    ONSIGHT
    FLASH
    REDPOINT
}

type Send {
    name: String
    style: Style
    grade: String
    tickType: TickType,
    location: String
}